import { PeymentFilterPipe } from './peyment-filter.pipe';

describe('PeymentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PeymentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
